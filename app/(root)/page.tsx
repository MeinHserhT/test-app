import React from "react";
// import { SheetRow, Player } from "@/src/types";
// import Table from "../components/table/table";
// import { getSheetById } from "@/src/getDatafunc";
// import Button from "../components/button/button";

const Home = async () => {
  // const json: SheetRow[] = await getSheetById("1258623711");
  // const data: Player[] = json.map((item: SheetRow) => new Player(item));
  // const headers: string[] = Object.keys(json[0]);
  // const years: string[] = Array.from(
  //   { length: 2026 - 2023 }, // new Date().getFullYear()
  //   (_, i) => (i + 2024).toString()
  // );

  // return (
  //   <div className="mt-8">
  //     <div className="px-8 flex flex-row justify-center">
  //       {years.map((y: string) => {
  //         return <Button key={y} title={y} link={"/" + y} />;
  //       })}
  //     </div>

  //     <Table data={data} headers={headers} />
  //   </div>
  // );
  return <div></div>;
};

export default Home;
